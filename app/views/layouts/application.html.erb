<!DOCTYPE html>
<html>
  <head>
    <title>Property Location</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload', defer: true %>
    <%# <%= stylesheet_pack_tag 'application' %>
    <%= favicon_link_tag asset_path('logo.jpg') %>
  </head>

  <body id="<%= controller.controller_name %>" class="<%=controller.action_name%> <%= "show-sidebar" if @show_sidebar.present? %> ">
    
    <% if @show_sidebar.nil? %> 
   <header>
      <%= render "layouts/nav/public"%>
        <%= yield %>  
    </header>
   <% else %>
     <%= render "layouts/nav/dashboard"%>

      <div class="container-fluid">
        <div class="row">

          <%= render "layouts/nav/sidebar" %> 
          <main role="main" class="col-md-9 ml-sm-auto col-lg-10 pt-3 px-4 mt-4">       
            <%= yield %>
          </main> 
        </div>
      </div>      

    <% end %>
    <div id="notification"><%= flash_notifications %></div>
  </body>
</html>